{
  "name": "loomal-cli",
  "version": "0.2.0",
  "description": "CLI for the Loomal API — identity infrastructure for AI agents",
  "bin": {
    "loomal": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "loomal",
    "cli",
    "ai",
    "agent",
    "email"
  ],
  "license": "MIT",
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
